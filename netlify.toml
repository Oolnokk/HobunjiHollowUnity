[build]
  # Static game; Netlify still installs package.json dependencies for Functions.
  publish = "docs"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

[build.processing]
  skip_processing = true
