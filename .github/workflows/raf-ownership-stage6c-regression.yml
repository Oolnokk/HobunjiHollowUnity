name: RAF ownership Stage 6 regression (social action body plane)

on:
  pull_request:
    paths:
      - 'docs/js/social-action-body-plane-runtime.js'
      - 'docs/js/runtime-frame-scheduler.js'
      - 'scripts/test-social-action-body-plane-scheduler.js'
      - '.github/workflows/raf-ownership-stage6c-regression.yml'
  push:
    branches: [main]
    paths:
      - 'docs/js/social-action-body-plane-runtime.js'
      - 'docs/js/runtime-frame-scheduler.js'
      - 'scripts/test-social-action-body-plane-scheduler.js'
      - '.github/workflows/raf-ownership-stage6c-regression.yml'

permissions:
  contents: read

jobs:
  regression:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Syntax check
        run: node --check docs/js/social-action-body-plane-runtime.js
      - name: Discovery/channel scheduler split
        run: node scripts/test-social-action-body-plane-scheduler.js
