name: RAF ownership Stage 6 regression (social action camera)

on:
  pull_request:
    paths:
      - 'docs/js/social-action-camera-runtime.js'
      - 'docs/js/runtime-frame-scheduler.js'
      - 'scripts/test-social-action-camera-scheduler.js'
      - '.github/workflows/raf-ownership-stage6d-regression.yml'
  push:
    branches: [main]
    paths:
      - 'docs/js/social-action-camera-runtime.js'
      - 'docs/js/runtime-frame-scheduler.js'
      - 'scripts/test-social-action-camera-scheduler.js'
      - '.github/workflows/raf-ownership-stage6d-regression.yml'

permissions:
  contents: read

jobs:
  regression:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Syntax check
        run: node --check docs/js/social-action-camera-runtime.js
      - name: Sentinel-maintenance scheduler migration
        run: node scripts/test-social-action-camera-scheduler.js
