name: Temporary deterministic den species patch

on:
  push:
    branches:
      - chatgpt/puktuk-western-den-predator
    paths:
      - 'scripts/_temp_patch_deterministic_den_species.py'
      - '.github/workflows/_temp-deterministic-den-species.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - name: Check out fix branch
        uses: actions/checkout@v4
        with:
          ref: chatgpt/puktuk-western-den-predator
          fetch-depth: 0
      - name: Apply source patch
        run: python scripts/_temp_patch_deterministic_den_species.py
      - name: Validate patched source
        run: |
          node --check docs/js/creature-genetics.js
          node --check docs/js/wildlife-spawn.js
          node --check docs/js/cavern-generator.js
          node scripts/test-puktuk-species.js
          node scripts/test-voorg-ass-species.js
      - name: Commit source patch
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add docs/js/wildlife-spawn.js docs/js/cavern-generator.js scripts/test-puktuk-species.js
          git commit -m 'Resolve one exact den species per den'
          git push origin HEAD:chatgpt/puktuk-western-den-predator
