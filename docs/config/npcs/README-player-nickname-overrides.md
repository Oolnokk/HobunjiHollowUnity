This directory contains a reviewable override layer for NPC-authored player nicknames.

`player-nickname-overrides.json` replaces the text of matching phrase-pool entries after the starter NPC database loads. It exists so small dialogue copy edits do not require rewriting the ~1 MB `hobunji-starter-npc-database.json` file.
